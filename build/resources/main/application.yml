#dev
run:
  mode: dev

spring:
  config:
    activate:
      on-profile: dev

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/quiztok?useTimezone=true&serverTimezone=UTC&elideSetAutoCommits=true
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: 1225
